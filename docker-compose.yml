services:
  caddy:
    image: sagarparker/oyster-fe-kit:latest
    network_mode: host
    volumes:
      - caddy_data:/data
      - caddy_config:/config

  dns-manager:
    image: sagarparker/dns-manager:latest
    network_mode: host
    environment:
      - DOMAIN_NAME=letsgoo.tech
    volumes:
      - caddy_data:/data

volumes: 
  caddy_data:
  caddy_config: 